name: Deploy Full Stack to EC2 (Server A)

on:
  push:
    branches:
      - main
    paths:
      - 'randevubu.server/**'
      - 'randevubu.client/**'
      - 'randevubu-infrastructure/server-a-api/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Deploy to Server A via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_A_IP }}
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/ubuntu/randevubu-infrastructure/server-a-api
            # Pull latest changes
            git pull origin main
            
            # Rebuild and restart all containers (Client, API, Nginx, Redis)
            docker-compose pull
            docker-compose up -d --build
