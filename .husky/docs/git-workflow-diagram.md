# Git Workflow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                     Developer Workflow                          │
└─────────────────────────────────────────────────────────────────┘

1. Make Changes
   │
   ├─ Edit TypeScript files
   ├─ Update Prisma schema
   └─ Modify documentation
   │
   ▼

2. Stage Changes
   │
   └─ git add .
   │
   ▼

3. Attempt Commit
   │
   └─ git commit -m "feat(auth): add 2FA"
   │
   ▼

┌─────────────────────────────────────────────────────────────────┐
│                    PRE-COMMIT HOOK (Husky)                      │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. TypeScript Type Check (tsc --noEmit)                  │  │
│  │    ✓ Check for type errors                               │  │
│  │    ✗ Fail if errors found                                │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          │                                      │
│                          ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 2. Code Formatting (Prettier)                            │  │
│  │    ✓ Format staged .ts files                             │  │
│  │    ✓ Format staged .json files                           │  │
│  │    ✓ Format staged .md files                             │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          │                                      │
│                          ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 3. Prisma Schema Formatting                              │  │
│  │    ✓ Format schema.prisma (if modified)                  │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
   │
   ├─ ✓ All checks passed
   │
   ▼

4. Write Commit Message
   │
   └─ Editor opens for commit message
   │
   ▼

┌─────────────────────────────────────────────────────────────────┐
│                  COMMIT-MSG HOOK (Commitlint)                   │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Validate Commit Message Format                           │  │
│  │                                                          │  │
│  │ Required Format:                                         │  │
│  │   <type>(<scope>): <subject>                            │  │
│  │                                                          │  │
│  │ Checks:                                                  │  │
│  │   ✓ Type is valid (feat, fix, refactor, etc.)          │  │
│  │   ✓ Subject is not empty                                │  │
│  │   ✓ Header ≤ 100 characters                             │  │
│  │   ✓ No period at end of subject                         │  │
│  │   ✓ Lowercase type and scope                            │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
   │
   ├─ ✓ Message valid
   │
   ▼

5. Commit Created! ✅
   │
   └─ Changes committed to local repository
   │
   ▼

6. Push to Remote
   │
   └─ git push origin main


┌─────────────────────────────────────────────────────────────────┐
│                      Example Flow                               │
└─────────────────────────────────────────────────────────────────┘

$ git add src/services/authService.ts
$ git commit -m "feat(auth): add JWT refresh token rotation"

Running pre-commit hooks...
  ✓ TypeScript type check passed
  ✓ Prettier formatting applied
  ✓ Prisma schema formatted

Validating commit message...
  ✓ Type 'feat' is valid
  ✓ Scope 'auth' is valid
  ✓ Subject is present
  ✓ Header length is 45 characters (≤ 100)

[main abc123] feat(auth): add JWT refresh token rotation
 1 file changed, 25 insertions(+), 5 deletions(-)


┌─────────────────────────────────────────────────────────────────┐
│                    Error Example                                │
└─────────────────────────────────────────────────────────────────┘

$ git commit -m "fixed bug"

Running pre-commit hooks...
  ✓ TypeScript type check passed
  ✓ Prettier formatting applied

Validating commit message...
  ✗ type may not be empty [type-empty]
  ✗ subject may not be empty [subject-empty]

✖ Commit message does not follow Conventional Commits format

Expected format:
  <type>(<scope>): <subject>

Examples:
  feat(auth): add two-factor authentication
  fix(appointments): resolve timezone issue

See docs/commit-conventions.md for details.


┌─────────────────────────────────────────────────────────────────┐
│                    Tools Installed                              │
└─────────────────────────────────────────────────────────────────┘

📦 Husky
   - Git hooks manager
   - Runs pre-commit and commit-msg hooks

📦 Lint-staged
   - Runs linters on staged files only
   - Faster than checking entire codebase

📦 Commitlint
   - Validates commit messages
   - Enforces Conventional Commits format

📦 Prettier
   - Code formatter
   - Ensures consistent code style

┌─────────────────────────────────────────────────────────────────┐
│                 Configuration Files                             │
└─────────────────────────────────────────────────────────────────┘

.husky/
  ├── pre-commit          # Runs lint-staged
  └── commit-msg          # Runs commitlint

.lintstagedrc.js          # Defines what to run on staged files
commitlint.config.js      # Commit message rules
.prettierrc               # Prettier formatting rules
.prettierignore           # Files to skip formatting

docs/
  ├── commit-conventions.md        # Full guide
  └── git-hooks-quick-reference.md # Quick reference
```
